% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_estimators.R
\name{ppi_multiclass}
\alias{ppi_multiclass}
\title{General multiclass PPI estimator with vector-valued g-transform.}
\usage{
ppi_multiclass(
  S_unlabeled,
  S_labeled,
  Y_labeled,
  g_transform,
  context,
  alpha = 0.05,
  project = TRUE
)
}
\arguments{
\item{S_unlabeled}{Integer vector of surrogate labels (test set).}

\item{S_labeled}{Integer vector of surrogate labels on calibration set.}

\item{Y_labeled}{Integer vector of true labels on calibration set.}

\item{g_transform}{Function mapping (S, context) to an n x K matrix.}

\item{context}{List passed into \code{g_transform} (must include \code{K}).}

\item{alpha}{Miscoverage level for per-class Wald intervals.}

\item{project}{Logical; if TRUE (default), the raw class-prevalence
estimates are projected onto the probability simplex so that they
are nonnegative and sum to one.}
}
\value{
A list containing per-class prevalence estimates, variance
estimates, and Wald confidence limits.
}
\description{
General multiclass PPI estimator with vector-valued g-transform.
}
